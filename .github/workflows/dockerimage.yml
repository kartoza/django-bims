name: Build and Push Docker Image

on: 
  push: 
    branches:
      - develop

jobs:

  build:
    name: Build and Push Docker Image
    runs-on: ubuntu-16.04
    steps:
      - uses: kartoza/kbims-healthyrivers/.github/workflows@develop
        env:
          secrets: ${{ secrets }}
      
