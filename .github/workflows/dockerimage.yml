name: Test Docker Image CI

on: 
  push: 
    branches:
      - develop

jobs:

  build:
 
    runs-on: ubuntu-16.04
 
    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --file deployment/production/Dockerfile --tag dimasciput/kartoza-django-bims:latest
